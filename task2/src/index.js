import React from 'react';
import ReactDOM from 'react-dom';
import App from '../src/components/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
    {/* <h6> Hi this is vishnu || index.js || this is for footer </h6> */}
  </React.StrictMode>,
  document.getElementById('root')
);
